[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: tests that interact with local sqlite or external services
    smoke: minimal real integration checks (Telegram/Google)
